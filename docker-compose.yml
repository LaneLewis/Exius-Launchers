version: "3"
services:
  exius:
    build: ./
    environment:
      - CONFIGNAME={...}
      - ADMINKEY={...}
      - DATABASE_URL=postgres://postgres:postgres@db:5432/postgres
    ports:
      - "8080:443"
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: "postgres"
    expose:
      - 5432